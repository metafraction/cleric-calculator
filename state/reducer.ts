import actions from "./actions";

const reducer = (state: any, action: any) => {
  if (action.type === actions.TOGGLE_OPTION) {
    // use action.key and set opposite of current state
    //
  }

  return state;
};

export default reducer;
